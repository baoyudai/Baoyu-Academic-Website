import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<React.StrictMode><App /></React.StrictMode>);


I've fixed the `index.html` link to Tailwind CSS and refined the code structure. Once you update these files on GitHub, your website's format will reappear instantly!
